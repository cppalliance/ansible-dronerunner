
Members of the "{{ dronerunner_developers_group }}" group may run these without a password:
  sudo apt / sudo apt-get   (any arguments)
  sudo {{ dronerunner_scripts_dir }}/startdronerunner.sh
  sudo {{ dronerunner_scripts_dir }}/stopdronerunner.sh

Anything else (this prompt) is not permitted. Please contact your administrator.

